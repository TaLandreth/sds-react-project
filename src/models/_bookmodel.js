export default class BookModel {
    constructor(id, author, title, genre, year) {
            this.id = id;
            this.author = author;
            this.title = title;
            this.genre = genre;
            this.year = year;
    }
}
